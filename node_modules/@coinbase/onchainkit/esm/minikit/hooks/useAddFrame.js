import sdk from '@farcaster/frame-sdk';
import { useCallback } from 'react';
import { useMiniKit } from './useMiniKit.js';

/**
 * Adds a frame to the user's farcaster account, enabling notifications.
 * @returns The notification details of the frame added or null if the frame was not added successfully.
 */
function useAddFrame() {
  const _useMiniKit = useMiniKit(),
    updateClientContext = _useMiniKit.updateClientContext;
  return useCallback(async () => {
    const result = await sdk.actions.addFrame();
    if (result.notificationDetails) {
      updateClientContext({
        details: result.notificationDetails,
        frameAdded: true
      });
      return result.notificationDetails;
    }
    return null;
  }, [updateClientContext]);
}
export { useAddFrame };
//# sourceMappingURL=useAddFrame.js.map
