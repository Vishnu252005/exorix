import sdk from '@farcaster/frame-sdk';
import { useCallback } from 'react';

/**
 * Closes the frame.
 * @returns void
 */
function useClose() {
  return useCallback(() => {
    sdk.actions.close();
  }, []);
}
export { useClose };
//# sourceMappingURL=useClose.js.map
